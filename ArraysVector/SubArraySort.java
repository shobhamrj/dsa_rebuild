package ArraysVector;

public class SubArraySort {
    // problem statement: find the sub array or two indices of those element in the array, which when sorted gives the sorted array as a whole

    // Brute force
    public static int[] bruteSubArraySort(int[] a) {
        
    } 
    
}
