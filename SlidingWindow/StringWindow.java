public class StringWindow {
    
    // given two strings one is called s(string) and other is called p(pattern) find a substring/window 
    // in the bigString that has smallString in it,it should be smallest substring. 
    // (order does not matter, but frequency of smallString matters also window can have additional characters)
    // example Input: Fizzbuzz, fuzz    Output: Fizzbu
    public static void findSubString(String string, String pattern) {
        // int[] FP = new int[256];
        // int[] FS = new int[256];
        // for(int i = 0; i < pattern.length(); i++) {

        // }

    }
}
